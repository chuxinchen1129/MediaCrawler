# 🚀 MediaCrawler 使用指南

## 快速开始

### 1️⃣ 小红书采集（最常用）

```bash
# 进入项目目录
cd /Users/echo/MediaCrawler

# 添加PATH（如果还没配置环境变量）
export PATH="$HOME/.local/bin:$PATH"

# 方式A: 使用默认配置（在config/base_config.py中设置关键词）
uv run main.py --platform xhs --lt qrcode --type search

# 方式B: 命令行指定关键词（推荐）
uv run main.py --platform xhs --lt qrcode --type search --keywords "你的关键词"

# 方式C: 自定义采集数量
uv run main.py --platform xhs --lt qrcode --type search --crawler_max_notes_count 50
```

### 2️⃣ 微博采集

```bash
cd /Users/echo/MediaCrawler
export PATH="$HOME/.local/bin:$PATH"

uv run main.py --platform wb --lt qrcode --type search --keywords "你的关键词"
```

### 3️⃣ 知乎采集

```bash
cd /Users/echo/MediaCrawler
export PATH="$HOME/.local/bin:$PATH"

uv run main.py --platform zhihu --lt qrcode --type search --keywords "你的关键词"
```

---

## 📝 配置文件修改

### 核心配置文件：`config/base_config.py`

```python
# ========== 必改配置 ==========

# 关键词（英文逗号分隔）
KEYWORDS = "无醇啤酒,0酒精啤酒"

# 采集数量
CRAWLER_MAX_NOTES_COUNT = 100

# ========== 可选配置 ==========

# 每篇笔记评论数
CRAWLER_MAX_COMMENTS_COUNT_SINGLENOTES = 30

# 请求间隔（秒）- 不建议低于3秒
CRAWLER_MAX_SLEEP_SEC = 5

# 输出格式：json, excel, csv, sqlite, db
SAVE_DATA_OPTION = "excel"
```

### 小红书专用配置：`config/xhs_config.py`

```python
# 排序方式
SORT_TYPE = "popularity_descending"  # 热门降序（点赞量最高）
# SORT_TYPE = "time_descending"         # 最新降序（时间最新）
```

---

## 🎯 常见使用场景

### 场景1: 快速测试（采集10条）

```bash
cd /Users/echo/MediaCrawler
export PATH="$HOME/.local/bin:$PATH"

uv run main.py --platform xhs --lt qrcode --type search \
  --keywords "测试关键词" \
  --crawler_max_notes_count 10
```

### 场景2: 大量采集（500条）

```bash
# 1. 修改配置文件
vim config/base_config.py
# 改为: CRAWLER_MAX_NOTES_COUNT = 500

# 2. 运行采集
uv run main.py --platform xhs --lt qrcode --type search
```

### 场景3: 采集用户主页

```bash
# 1. 修改 config/xhs_config.py
vim config/xhs_config.py
# 添加用户主页链接到 XHS_CREATOR_ID_LIST

# 2. 运行采集
uv run main.py --platform xhs --lt qrcode --type creator
```

### 场景4: 多关键词批量采集

```bash
# 修改配置文件
vim config/base_config.py
# KEYWORDS = "关键词1,关键词2,关键词3"

# 运行
uv run main.py --platform xhs --lt qrcode --type search
```

---

## 📂 数据存储位置

### Excel文件（推荐）
```
/Users/echo/MediaCrawler/data/xhs/
└── xhs_search_20250126_163000.xlsx
    ├── Sheet1: Contents（笔记内容）
    ├── Sheet2: Comments（评论内容）
    └── Sheet3: Creators（作者信息）
```

### JSON文件
```
/Users/echo/MediaCrawler/data/xhs/search/
└── contents_20250126.json
```

---

## 🛠️ 辅助工具

### 数据合并去重
```bash
# 合并小红书数据
python3 /Users/echo/merge_excel_data.py xhs

# 合并所有平台
python3 /Users/echo/merge_excel_data.py --all
```

### 数据分析
```bash
python3 /Users/echo/analyze_data.py /Users/echo/MediaCrawler/data/xhs/xhs_search_20250126_163000.xlsx
```

### 批量采集
```bash
# 查看帮助
/Users/echo/batch_crawl.sh

# 采集小红书（默认20条）
/Users/echo/batch_crawl.sh xhs

# 自定义关键词和数量
/Users/echo/batch_crawl.sh xhs "你的关键词" 50
```

---

## ⚠️ 常见问题

### Q1: 登录失败
**解决**:
- 确保扫码登录成功
- 检查网络连接
- 尝试重新运行

### Q2: 采集速度慢
**说明**: 正常现象，为了反爬虫保护，每条笔记间隔5秒

### Q3: 461状态码（反爬虫检测）
**解决**:
- 增加延迟时间：`CRAWLER_MAX_SLEEP_SEC = 10`
- 等待1-2小时后再试
- 启用IP代理（需配置）

### Q4: 浏览器无法启动
**解决**:
- 检查Chrome是否已安装
- 关闭所有Chrome窗口后重试
- 端口9222被占用：重启电脑或修改端口

---

## 🔑 登录态保持

首次扫码登录后，登录信息会保存在：
```
/Users/echo/MediaCrawler/browser_data/cdp_xhs_user_data_dir/
```

下次运行时会自动使用，无需重新登录（有效期约7-30天）

---

## 📋 完整命令参考

### 平台参数
- `--platform xhs` - 小红书
- `--platform wb` - 微博
- `--platform zhihu` - 知乎

### 登录方式
- `--lt qrcode` - 扫码登录（推荐）
- `--lt cookie` - Cookie登录
- `--lt phone` - 手机号登录

### 采集类型
- `--type search` - 关键词搜索
- `--type detail` - 指定笔记详情
- `--type creator` - 用户主页

### 其他参数
- `--keywords` - 关键词
- `--crawler_max_notes_count` - 采集数量
- `--save_data_option` - 存储格式

---

## 💡 最佳实践

### 1. 采集前
- ✅ 检查配置文件参数
- ✅ 确认网络连接稳定
- ✅ 关闭不必要的Chrome窗口

### 2. 采集中
- ✅ 保持窗口打开
- ✅ 耐心等待（不要频繁中断）
- ✅ 观察日志输出

### 3. 采集后
- ✅ 检查Excel文件完整性
- ✅ 使用辅助工具处理数据
- ✅ 及时备份数据

---

## 📞 需要帮助？

查看详细文档：
```bash
cat /Users/echo/MediaCrawler/README.md
```

查看项目地址：
- GitHub: https://github.com/NanmiCoder/MediaCrawler
- 问题反馈: https://github.com/NanmiCoder/MediaCrawler/issues

---

**最后更新**: 2025-01-26
**版本**: v1.0
